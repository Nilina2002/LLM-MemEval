"use client";

export default function SettingsPage() {
  return (
    <div className="space-y-4 max-w-xl">
      <h1 className="text-2xl font-bold">Settings</h1>
      <p className="text-muted-foreground">Configure default LLM providers and experiment parameters.</p>
      {/* TODO: settings form — LLM defaults, API key validation, result folder */}
    </div>
  );
}
